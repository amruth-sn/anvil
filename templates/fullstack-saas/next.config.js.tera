/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '{{ domain | default(value="*") }}',
      },
    ],
  },
}

module.exports = nextConfig